코딩 인터뷰 완전분석
1. 자료구조 - 연결 리스트

    1) 연결 리스트 생성
        - 단방향 / 양방향 연결 리스트인지 확실히 해야 한다.

    2) 단방향 연결 리스트에서의 노드 삭제
        - 유의할 점
            (1) 널 포인터 검사는 반드시 해야 한다.
            (2) 필요하다면 head와 tail 포인터도 갱신해야 한다.

    3) "Runner" 기법 : 부가 포인터라고도 한다.
          - 연결 리스트를 순회할 때 두 개의 포인터를 동시에 사용하는 것이다.

Q : Page. 96  /  A : Page. 234

2.1 비정렬 연결 리스트에서 중복 문자를 제거하는 코드를 작성하라.
    - 연관문제 : 임시 버퍼가 허용되지 않는 상황에서 이 문제를 어떻게 해결할 수 있겠는가?

2.2 단방향 연결 리스트에서, 뒤에서 k번째 원소를 찾는 알고리즘을 구현하라.

2.3 단방향 연결 리스트의 중간에 있는 노드 하나를 삭제하는 알고리즘을 구현하라. 삭제할 노드에 대한 접근만 가능하다는 것에 유의하라.
        ex)
            입력 : 연결 리스트 a->b->c->d->e 의 노드 c
            결과 : 아무것도 반환할 필요 없고, 결과로 연결리스트가 a->b->d->e가 되어 있으면 OK

2.4 x 값을 갖는 노드를 기준으로 연결 리스트를 나누는 코드를 작성하라.
    x보다 작은 값을 갖는 노드가 x와 같거나 더 큰 값을 갖는 노드들보다 앞족에 오도록 하면 된다.

2.5 연결 리스트로 표현된 두 개의 수가 있다고 하자.
    리스트의 각 노드는 해당 수의 각 자릿수를 표현한다.
    이때 자릿수들은 역순으로 배열되는데, 1의 자릿수가 리스트의 맨 앞에 오도록 배열된다는 뜻이다.
    이 두 수를 더하여 그 합을 연결 리스트로 반환하는 함수를 작성하라.
        ex)
            입력 : (7->1->6) + (5->9->2). 즉, 617+295
            결과 : 2->1->9. 즉 912
    - 연관문제 : 각 자릿수가 정상적으로 배열된다고 가정하고 구현해 보자.
        ex)
            입력 : (6->1->7) + (2->9->5). 즉, 617+295
            결과 : 9->1->2. 즉 912

2.6 순환 연결 리스트(circular linked list)가 주어졌을 때, 순환되는 부분의 첫 노드를 반환하는 알고리즘을 작성하라.
    - 정의
        순환 연결 리스트 : 순환 연결 리스트는 노드의 next 포인터가 앞선 노드들 가운데 어느 하나를 가리키도록 설정되어 있는 연결리스트이다.
        ex)
            입력 : A->B->C->D->E (E의 next 노드가 D 앞에 있는 C로 설정되어 있다.)
            출력 : C

2.7 주어진 연결 리스트가 회문인지 검사하는 함수를 작성하라.



